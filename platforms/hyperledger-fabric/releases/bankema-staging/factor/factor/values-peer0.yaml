apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: factor-peer0
  namespace: factor-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  releaseName: factor-peer0
  chart:
    git: git@github.com:sivaramkannan/blockchain-automation-framework.git
    ref: bankema-staging
    path: platforms/hyperledger-fabric/charts/peernode    
  values:
    metadata:
      namespace: factor-net
      images:
        couchdb: hyperledger/fabric-couchdb:0.4.18
        peer: hyperledger/fabric-peer:2.2.0
        alpineutils: index.docker.io/hyperledgerlabs/alpine-utils:1.0
        
    peer:
      name: peer0
      gossippeeraddress: peer0.factor-net:7051
      gossipexternalendpoint: peer0.factor-net.factor-net.staging.bankema.emulya.com:8443
      localmspid: factorMSP
      loglevel: info
      tlsstatus: true
      builder: hyperledger/fabric-ccenv:2.2.0
      couchdb:
        username: factor-user

    storage:
      peer:
        storageclassname: factorsc
        storagesize: 10Gi
      couchdb:
        storageclassname: factorsc
        storagesize: 5Gi

    vault:
      role: vault-role
      address: http://bankemastaging.westeurope.cloudapp.azure.com:8200
      authpath: factor-net-auth
      secretprefix: secret/crypto/peerOrganizations/factor-net/peers/peer0.factor-net
      secretambassador: secret/crypto/peerOrganizations/factor-net/ambassador
      serviceaccountname: vault-auth
      imagesecretname: regcred
      secretcouchdbpass: secret/credentials/factor-net/couchdb/factor?user

    service:
      servicetype: ClusterIP
      ports:
        grpc:
          clusteripport: 7051
        events:
          clusteripport: 7053
        couchdb:
          clusteripport: 5984
          
    proxy:
      provider: haproxy
      external_url_suffix: factor-net.staging.bankema.emulya.com
